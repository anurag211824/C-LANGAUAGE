#include <stdio.h>

int main()
{
    int i = 0;
    // do while loop -----> first excute code then the check the condition.
    do
    {
        printf("the value of i is %d\n", i);
        i++;

    } while (i <= 10);

    return 0;
}