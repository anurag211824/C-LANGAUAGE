#include <stdio.h>
// write a programme to print natural numbers from 10 to 20,
// when intial loop counter i is intilized to 0
int main()
{
    int i=10;
    
    while (i <= 20)
    {
        if (i >= 10)
        {
            printf("the value of i is %d \n", i);
        }
        
    i++;}
    // i is increment operator.
    return 0;
}