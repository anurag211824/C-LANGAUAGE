#include <stdio.h>

int main()
{
    // int a[] = {34, 232, 23};
    // printf("the vqalue of a[0] is %d\n",a[0]);
    // printf("the vqalue of a[0] is %d\n",a[1]);
    // printf("the vqalue of a[0] is %d\n",a[2]);
    float a[] = {34, 232, 23};
    printf("the value of a[0] is %.2f\n", a[0]);
    printf("the value of a[1] is %.2f\n", a[1]);
    printf("the value of a[2] is %.2f\n", a[2]);

    return 0;
}